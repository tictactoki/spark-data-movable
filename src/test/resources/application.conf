{
    movable {

        aws {
            region = "eu-west-3"
        }

        spark {
            is_local_job = true
            worker_number = "*"
        }

        dbs {
            localhost {
                games {
                    type = "postgresql"
                    host = "localhost"
                    port = 5432
                    db = "games"
                    username = "wong"
                    #password = "postgres"
                }
                dbs2 {
                    host = "0.0.0.0"
                    port = 5432
                    db = "movabledb"
                    username = "postgres"
                    password = "postgres"
                }
            }

            server2 {
                dbs1 {
                    host = "0.0.0.0"
                    port = 5432
                    db = "movabledb"
                    username = "postgres"
                    password = "postgres"
                }
            }

        }

        files {

            localhost {
                input_file_format = "csv"
                input_path = "input_path"
                output_file_format = "parquet"
                output_path = "output_path"
            }

            without_input {
                output_file_format = "parquet"
                output_path = "output_path"
            }
        }
    }
}